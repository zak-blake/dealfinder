<h4>
  <%= @event.name %>
</h4>

<h4>
  <%= @event.description %>
</h4>

<h5>
<%= "From " + @event.start_time.to_s(:time) + " To " + @event.end_time.to_s(:time) %>
</h5>
<%= button_to "delete", @event, :method => :delete %>
