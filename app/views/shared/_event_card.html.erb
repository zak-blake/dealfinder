<div class="card">
  <h4>
    <%= link_to event.name, event %>
  </h4>

  <h4 style="float: right">
    <small><%= event.start_time.strftime("%I:%M%p") + " to " + event.end_time.strftime("%I:%M%p") %></small>
  </h4>

  <p>
    <%= event.description %>
  </p>
</div>
